# Backend API URL
# Defaults to relative URL '/api' which goes through Caddy reverse proxy
# In development: localhost:1314/api → Caddy → backend:8000
# In production: yourdomain.com/api → Caddy → backend:8000
# Only override if you need a different URL
# VITE_API_URL=/api
