# ============================================
# ENVIRONMENT CONFIGURATION
# ============================================
# Options: "development" or "production"
# Production mode enforces strict security settings
ENV=development

# ============================================
# SECURITY CONFIGURATION
# ============================================
# REQUIRED: Generate a random secret key (at least 32 characters)
# Generate with: openssl rand -hex 32
SECRET_KEY=generate-a-random-secret-key-here

# Secure cookies (REQUIRED in production, requires HTTPS)
# Development: false (HTTP allowed)
# Production: true (HTTPS only)
SECURE_COOKIES=false

# ============================================
# DATABASE CONFIGURATION
# ============================================
POSTGRES_DB=bulq
POSTGRES_USER=bulq
POSTGRES_PASSWORD=change-this-password
DATABASE_URL=postgresql://bulq:change-this-password@db:5432/bulq

# Database Connection Pool Configuration
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600

# Repository Mode
# Options: "memory" (test data) or "database" (PostgreSQL)
# Production: MUST be "database"
REPO_MODE=memory

# ============================================
# SESSION CONFIGURATION
# ============================================
SESSION_EXPIRY_HOURS=24

# ============================================
# CORS CONFIGURATION
# ============================================
# REQUIRED in production: Comma-separated list of allowed origins
# Development: Leave empty for defaults (localhost:3000, localhost:5173)
# Production: Set to your domain(s)
# Examples:
#   Single domain: ALLOWED_ORIGINS=https://yourdomain.com
#   Multiple domains: ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=

# ============================================
# DOMAIN CONFIGURATION (Production)
# ============================================
# Leave empty for development (serves on port 3000)
# Set in production for automatic HTTPS/SSL with Let's Encrypt
# Example: DOMAIN=yourdomain.com
DOMAIN=

# Port configuration (optional, defaults shown)
FRONTEND_PORT=3000
HTTP_PORT=80
HTTPS_PORT=443

# ============================================
# LOGGING CONFIGURATION
# ============================================
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
# Log format: "structured" (key=value pairs) or "json" (production)
LOG_FORMAT=structured
# Log file path (leave empty for console only, or set to /app/logs/app.log)
LOG_FILE=
